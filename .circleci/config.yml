version: 2.1
executors:
  default:
    working_directory: ~/workspace
    docker:
      - image: circleci/node:12
jobs:
  build:
    executor:
      name: default
    steps:
      - checkout
      - run: npm install
      - run: echo "hello world"
  test:
    executor:
      name: default
    steps:
      - run: npm test
